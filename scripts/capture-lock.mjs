import {chromium} from 'playwright';
const b=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});const p=await b.newPage({viewport:{width:1920,height:1080}});await p.goto('http://localhost:3001/media/part2-bag.mp4');await p.evaluate(async()=>{const v=document.querySelector('video');v.pause();v.controls=false;document.body.style.margin='0';v.style.cssText='position:fixed;inset:0;width:100vw;height:100vh;object-fit:cover';if(v.readyState<2)await new Promise(r=>v.addEventListener('loadeddata',r,{once:true}));v.currentTime=10;await new Promise(r=>v.addEventListener('seeked',r,{once:true}));});await p.screenshot({path:'assets/source/film-lock-browser.png'});await b.close();

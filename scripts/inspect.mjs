import { chromium } from 'playwright';
const browser=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true,args:['--remote-debugging-port=9333','--enable-unsafe-swiftshader']});
const page=await browser.newPage({viewport:{width:1440,height:900}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://localhost:3001');await page.waitForTimeout(5000);await page.screenshot({path:'review/desktop-opening.png'});console.log(await page.evaluate(()=>({canvas:document.querySelectorAll('canvas').length,active:document.querySelector('.experience')?.className,overflow:document.documentElement.scrollWidth>innerWidth,video:[...document.querySelectorAll('video')].map(v=>({src:v.currentSrc,ready:v.readyState})),height:document.querySelector('.hero-runway')?.getBoundingClientRect().height})));console.log(errors);
await page.evaluate(()=>scrollTo(0,document.querySelector('.hero-runway').getBoundingClientRect().height*.445));await page.waitForTimeout(2500);await page.screenshot({path:'review/desktop-seam.png'});console.log(await page.locator('.experience').getAttribute('data-progress'));await browser.close();
